# koa-server 项目构建
```js

1. 全局安装安装 `koa-generator`： 
 npm install -g koa-generator
2. 使用 `koa-generator` 生成 `koa2` 项目： 
 koa2 koa-server
3. cd 到文件目录安装依赖并且启动项：
 npm install
 npm start

```
